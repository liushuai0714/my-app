import ThemedButton from './ClassThemeButton'
import ThemeLink  from './ThemeLink'

const Toolbar = () => {
    return (
        <div>
            <ThemedButton />
            <ThemeLink />
        </div>
    )
}

export default Toolbar